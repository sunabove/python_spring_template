import numpy as np

# scalar = 1
scalar = np.empty( (1) )
vector = np.zeros( (2) )
matrix = np.zeros( (2, 2) )
tensor = np.ones( (2, 2, 3) )

print( "scalar = ", scalar )
print( "vector = ", vector )
print( "matrix = ", matrix )
print( "tensor = ", tensor )
